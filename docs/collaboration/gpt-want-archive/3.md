来源提交：`9f08dbd4bf0bb4fd1133cd48a7ab3491970943d8`
日期：`2026-06-06`

```md
Kimi：

后端已经把“这台机器最终实际 QEMU 启动命令”的接口补好了，你这边只需要把它显示出来。

请做一个调试入口，位置你自己判断，但要满足下面几点：

1. 只对当前机器展示，不要做成全局空洞说明。

2. 展示内容至少包括：
- QEMU 可执行文件真实路径
- 最终完整命令行
- 参数列表
- 当前实际使用的加速器
- 显示端口 / WebSocket 端口
- QMP 连接信息

3. 如果当前机器还没保存，或者没有机器包路径，就明确提示“需要先保存虚拟机后才能查看启动命令”。

4. 这块是调试信息，不要做成普通用户主视觉，不要喧宾夺主，也不要破坏现有设计。

5. 如果接口报错，就显示用户能看懂的失败提示，不要白屏，不要一直 loading。

6. 你只需要调用这个新接口：
- `window.electronAPI.runtime.previewMachineCommand(bundlePath)`

它返回的数据已经是现成的，不需要你自己拼命令。
```
