来源提交：`847f31784bfa905bb06ca77194ea46237f602b3a`
日期：`2026-06-07`

```md
# GPT -> Kimi

这轮前端任务只做控制台状态栏收纳和下拉菜单，不要自己扩设计。

## 目标

控制台右侧状态栏位置不够了。

把：

- 共享文件夹按钮

从顶栏直接移掉，改成一个：

- 下拉图标按钮

点开后显示下拉菜单。

## 这轮你要做的内容

1. 控制台状态栏增加一个下拉菜单图标

2. 下拉菜单里目前先只放一个菜单项：

- `检测虚拟机网络 (Windows)`

3. 点击这个菜单项后：

- 不要你自己拼项目根目录路径
- 不要自己再走文件选择器
- 直接调用后端已经提供好的接口：

`window.electronAPI.runtime.mountBundledTestNetIso(machineId)`

这个接口会自动处理：

- 开发环境下的 `testnet.iso`
- 打包环境下资源里的 `testnet.iso`
- 实际换盘逻辑

## 你不要做的事

- 不要保留原来的“共享文件夹”顶栏按钮
- 不要自己新增第二套换盘逻辑
- 不要自己找 `testnet.iso` 路径
- 不要改控制台整体设计语言
- 不要自己加一堆菜单项
- 不要改成抽屉、弹窗、侧栏

## 交互要求

- 下拉菜单样式保持克制
- 图标、间距、交互和现在控制台顶栏一致
- 如果点击菜单项后失败，沿用现有错误反馈，不要自己再造一套

## 你做完后回给我

请写一份新的 `kimi-want.md`，告诉我：

1. 你把下拉图标放在哪
2. 你动了哪些文件
3. 你怎么接 `mountBundledTestNetIso(machineId)`
4. 你有没有顺手动到别的控件
```
